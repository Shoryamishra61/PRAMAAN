# T020 acceptance evidence

Requirements: `PRD-009`, `PRD-010`, `API-002`, `API-003`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_case_api.py -q
cd frontend
npm test
```

Observed results: 4 backend API tests and 2 frontend component tests passed. `GET /api/v1/cases` returns the contracted raw reason/profile/processing/gate fields with REVIEW/BLOCK-before-PASS ordering, filters, and stable cursor pagination. `GET /api/v1/cases/{case_id}` returns normalized payment/refund state, synthetic evidence text and digests, grounded claims, findings, latest decision, workflow state, and audit events, with no provider raw response or secret fields.

The React interface fetches those local endpoints through the Vite proxy and renders a keyboard-selectable queue plus responsive evidence/findings workspace. It labels synthetic/local regex mode, preserves raw `reason_code`, formats integer minor units exactly, states the decision-support boundary, and exposes no accept/contest/refund/payment write control.

A real defect was encountered: replacing `sqlite3.Row.keys()` iteration with direct row iteration caused hydration to use row values as keys. The implementation now converts rows to dictionaries before field extraction, and the full API workspace test is the regression guard.

Full `scripts/check.ps1` result: 137 backend tests and 2 frontend tests passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
