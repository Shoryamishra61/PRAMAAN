# T013 acceptance evidence

Requirements: `PRD-013`, `PRD-017`, `SRS-017`, `AI-007`, `AI-011`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_semantic_pipeline.py -q
```

Observed result: 9 passed, including a Hypothesis failure-safety property.

Transient/timeout failures retry only to the configured bound. Exhaustion and unexpected failures produce `F_MODEL_UNAVAILABLE`; invalid schema/disallowed claims and unsupported inputs produce `F_SOURCE_UNSUPPORTED`; ambiguous/not-found grounding produces `F_SOURCE_UNGROUNDED`; unresolved normalization produces REVIEW. All generated failure findings have REVIEW effect, and unsupported input does not invoke the extractor.

Full `scripts/check.ps1` result: 97 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

