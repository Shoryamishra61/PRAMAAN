# T022 acceptance evidence

Requirements: `PRD-013`, `API-004`.

Verified on 2026-08-23.

`POST /api/v1/cases/{case_id}/reprocess` uses the configured fixed demo operator identity, accepts an optional `Idempotency-Key`, and atomically appends `REPAIR_REQUESTED`, creates one durable `PENDING` processing job, and returns the case to local review/queued state. Duplicate keys return the same logical job. Missing cases and blank keys have structured errors. Every success response records `network_write_performed: false`.

The REVIEW panel maps the specified failure classes to concrete explanations and recovery steps without model-confidence language. Its recovery control calls only the local reprocess endpoint and visibly confirms the durable job ID and no-network-write boundary.

```powershell
python -m pytest backend/tests/test_case_api.py -q
cd frontend
npm test
```

Observed results: 7 backend API tests and 4 frontend workflow tests passed. Full `scripts/check.ps1` result: 140 backend tests and 4 frontend tests passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
