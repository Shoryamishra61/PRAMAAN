# T024 acceptance evidence

Requirements: `PRD-014`, `SRS-019`, `API-008`.

Verified on 2026-08-23.

`GET /api/v1/evaluation/latest` scans only saved versioned JSON runs, verifies each exact-byte `.sha256` sidecar, validates the closed artifact schema, selects the newest recorded timestamp, and projects dataset/system/metric provenance without recomputation. Missing artifacts return exactly `{"status":"NOT_YET_MEASURED"}`; tampering returns a structured server error.

The evaluation view renders the synthetic warning, dataset/split/hash, extractor/config/code revision, saved ratios with numerator/denominator counts, confusion matrix, slices, baseline delta, and illustrative cost inputs only when present in the response. Without an artifact it explicitly shows no values. Test-only fixture metrics are labeled fixtures and are not committed as claimed results.

```powershell
python -m pytest backend/tests/test_evaluation_api.py -q
cd frontend
npm test
```

Observed results: 3 backend artifact API tests and 7 frontend tests passed. Full `scripts/check.ps1` result: 144 backend tests and 7 frontend tests passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
