# T003 acceptance evidence

Requirements: `PRD-003`, `PRD-007`, `POL-001`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_demo_fixtures.py -q
```

Observed result: 7 passed.

The fixtures use the documented Razorpay `payment.dispute.created` envelope and documented dispute/payment fields, preserve a synthetic raw `reason_code` distinct from the local `refund_not_processed_v1` profile, and cover expected PASS, REVIEW, and BLOCK states. They live under `data/demo/`, outside any benchmark holdout path.

Full `scripts/check.ps1` result: 15 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

