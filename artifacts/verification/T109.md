# T109 acceptance evidence

## Requirements

`PRD-009`, `PRD-011`, `PRD-014`, `PRD-017`, `PRD-021`, `SRS-019`,
`SRS-021`, `SRS-025`, `SRS-026`, `AI-015`, `AI-016`.

## Acceptance tests

- default route is the Track 02 proof console, not a landing/tutorial or queue;
- canonical BLOCK case facts, raw `reason_code`, exact grounded quote, trusted
  ledger state, deterministic finding, and local human handoff load from local
  APIs;
- selected B0 extractor, grouped DEV-only local ML challenger,
  `NOT_PROMOTED`, comparator result, and signed n-gram contributions are visible
  without exposing model probability or gate authority;
- precision, recall, F1, false-PASS, REVIEW, confusion matrix, and cost counts
  load only from the saved evaluation response;
- illustrative cost controls recompute from the saved false-PASS,
  false-BLOCK, and REVIEW counts and are labeled unitless;
- architecture matches the implemented signed-ingest, durable-job, extraction,
  grounding, deterministic-verifier, local-workflow, and artifact path;
- recorded semantic failure is shown as REVIEW and recovery re-establishes the
  deterministic BLOCK case;
- analyst evidence handoff remains functional;
- no Razorpay accept/contest/refund/payment write exists.

## Automated evidence

`powershell -ExecutionPolicy Bypass -File scripts/check.ps1` passed on
2026-08-23:

- 174 backend tests passed;
- 11 frontend interaction tests passed;
- Ruff formatting and lint passed;
- strict mypy passed;
- specification, package, source, and JSON-contract validation passed;
- the no-Razorpay-write guard passed;
- Prettier and ESLint passed;
- TypeScript/Vite production build passed.

## Browser/design evidence

- current-run audit: `artifacts/ux-audit/judge-visibility-audit/audit.md`;
- selected target: Superdesign draft
  `8da1064a-d768-44d8-a7cf-cc61bcb4ea13`, version 6;
- browser implementation capture:
  `artifacts/ux-audit/judge-visibility-audit/13-implementation-910x642.png`;
- responsive capture:
  `artifacts/ux-audit/judge-visibility-audit/15-implementation-mobile.png`;
- `design-qa.md` final result: passed;
- browser console warnings/errors: none;
- tested interactions: replay, four proof chapters, cost recomputation,
  recorded recovery, and analyst handoff.

No frozen HOLDOUT data was modified or rerun for this UI-only change.
