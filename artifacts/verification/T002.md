# T002 acceptance evidence

Requirements: `PRD-002`, `SRS-005`, `SRS-009`, `SRS-010`, `SRS-016`.

Verified on 2026-08-23.

Targeted command:

```powershell
python -m pytest backend/tests/test_database.py -q
```

Observed result: 5 passed.

Full command:

```powershell
& .\scripts\check.ps1
```

Observed result: Ruff format/lint passed, strict mypy passed, 8 backend tests passed, frontend format/lint/build passed, 1 frontend test passed, exit code 0.

Acceptance coverage includes idempotent schema initialization, WAL mode, per-connection foreign keys, required tables/indexes, raw `reason_code` preservation, uppercase currency normalization, UTC serialization, rejection of float money and naive timestamps, database constraints, and job persistence across connections.

