# T010 acceptance evidence

Requirements: `PRD-001`, `PRD-NFR-002`, `SRS-002`, `SRS-016`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_webhook_replay.py -q
```

Observed result: 3 passed.

The suite signs and replays exact fixture bytes, measures a 10-request local ACK sample and asserts inclusive p95 below 5 seconds, verifies durable rows after ACK, verifies replay deduplication through the existing ingestion tests, rejects a raw-body mutation signed with the original digest before database creation, and reclaims a persisted leased job after a simulated process restart. `scripts/replay_webhook.py` reads its secret only from `DIG_WEBHOOK_SECRET` and never prints it.

Full `scripts/check.ps1` result: 59 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

