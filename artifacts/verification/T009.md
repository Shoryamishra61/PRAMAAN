# T009 acceptance evidence

Requirements: `SRS-016`, `SRS-017`, `PRD-017`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_jobs.py -q
```

Observed result: 5 passed.

Tests prove that a live lease prevents a second claim, a stale PROCESSING job is reclaimed after a simulated worker restart under the same job identity, successful handling completes once, transient failures stop at the configured attempt bound, and permanent failures are not retried.

Full `scripts/check.ps1` result: 56 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

