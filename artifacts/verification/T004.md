# T004 acceptance evidence

Requirements: `PRD-003`, `PRD-008`, `POL-001`, `POL-003`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_profile.py -q
```

Observed result: 4 passed.

The executable profile is strictly loaded from `contracts/refund_not_processed_v1.yaml`. Tests prove that suggested evidence remains labeled as suggested, missing categories are exposed for REVIEW, unsupported local profiles resolve to `OUT_OF_SCOPE`, and raw Razorpay code `RZP04` is not treated as the local profile ID.

Full `scripts/check.ps1` result: 19 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

