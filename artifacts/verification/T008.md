# T008 acceptance evidence

Requirements: `PRD-001`, `SRS-003`, `SRS-004`, `API-001`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_ingestion.py -q
```

Observed result: 6 passed.

The integration tests prove that authentication precedes parsing; a documented `payment.dispute.created` event atomically persists event, normalized case, and durable job; body SHA-256 and raw `reason_code` are retained; extra fields are tolerated; duplicate `x-razorpay-event-id` deliveries return safe 202 responses with one logical event/case/job; and a documented non-MVP dispute event is durably recorded without scheduling business processing.

Full `scripts/check.ps1` result: 51 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

