# T018 acceptance evidence

Requirements: `PRD-014`, `SRS-019`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_evaluation_artifact.py -q
```

Observed result: 5 passed. `contracts/evaluation-result.schema.json` closes the artifact envelope and records the run timestamp, system/extractor/model/prompt/schema identifiers, config SHA-256, code revision, synthetic dataset ID/version/split/manifest digest, unique case predictions, expected labels/claims for evaluation, slices, and metrics payload.

The writer serializes canonical, versioned JSON, hashes the exact saved bytes, writes a `.sha256` sidecar, rejects unsafe run IDs, and refuses to overwrite an existing run. Config hashing is path-and-byte-sensitive and order-independent. Because this workspace is not a Git repository, test provenance explicitly records `UNAVAILABLE_NOT_A_GIT_REPOSITORY`; no commit value was fabricated.

Full `scripts/check.ps1` result: 128 backend tests and 1 frontend test passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
