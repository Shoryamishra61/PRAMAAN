# T019 acceptance evidence

Requirements: `PRD-014`, `PRD-015`, `AI-013`, `SRS-019`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_evaluation_metrics.py -q
```

Observed result: 5 passed, including a Hypothesis property test. All values are computed from case prediction records: material-conflict precision/recall/F1 with numerator and denominator counts; false-PASS/false-BLOCK counts; REVIEW rate; automatic-decision coverage; three-class confusion matrix and macro-F1; exact claim-type micro/macro scores; exact grounding rate; normalized-value accuracy; and per-slice counts.

Baseline deltas apply the same metric contract to both inputs. Cost sensitivity accepts visible parameters and reports unitless `illustrative_cost_units` for review-cheap, balanced, and false-pass-expensive examples; it does not infer Razorpay fees, INR savings, or production costs.

Full `scripts/check.ps1` result: 133 backend tests and 1 frontend test passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
