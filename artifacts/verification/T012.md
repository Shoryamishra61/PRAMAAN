# T012 acceptance evidence

Requirements: `PRD-005`, `PRD-011`, `SRS-008`, `AI-004`, `AI-005`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_grounding.py -q
```

Observed result: 19 passed, including a Hypothesis money property.

Unique exact quotes resolve to backend-generated exclusive character spans; missing, whitespace-altered, and repeated quotes resolve to UNGROUNDED/AMBIGUOUS with no offsets. INR values are normalized with `Decimal` into integer paise; unsupported currencies, ambiguous amounts, naive dates, and partial date text remain unresolved. RFC3339 timestamps require explicit timezone and normalize to UTC. Refund references accept bounded identifier tokens only.

Full `scripts/check.ps1` result: 88 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

