# T030 acceptance evidence

Requirements: `PRD-014`, `PRD-016`, `SRS-019`.

Verified on 2026-08-23.

The implementation README now separates working local behavior, synthetic fixtures/benchmark, mocked merchant resolver data, deliberately unimplemented live/Razorpay write capabilities, and future work. Every stated performance, count, timing, and illustrative cost value is transcribed from the digest-verified final artifact and identifies that artifact's SHA-256. It reports the 10 false PASS cases and the zero-correct `partial_full_amount` slice prominently; it does not claim savings, production prevalence, win lift, legal correctness, or a model-backed B1 result.

The live analyst UI was exercised against `results/holdout-regex-v1-20260823-final.json`. The dashboard displayed the synthetic warning; HOLDOUT dataset/version; regex extractor; unavailable Git provenance; artifact/config hashes; 10/10 precision, 10/20 recall, 20/60 REVIEW, 40/60 coverage, 10 false PASS, 0 false BLOCK; confusion matrix; error slices; zero identical-B0 baseline delta; and three unitless cost-sensitivity scenarios. These values came through `GET /api/v1/evaluation/latest`, which reverified the sidecar and projected the saved artifact without recomputation.

The same browser session confirmed the visible `OFFLINE REPLAY · PRECOMPUTED REGEX` badge, synthetic evidence label, REVIEW/BLOCK/PASS queue rows, preserved raw reason codes, local-only/no-write copy, the BLOCK evidence/ledger comparison, and focus movement to the exact grounded source quote.

Final artifact SHA-256: `15349fd24f2fbceb1c6a38edafee92d5953f22af2e9611efcda17ba20f1992b8`.

No detector, evaluator, prompt, schema, or policy byte was changed after the release freeze. The HOLDOUT was not rerun.
