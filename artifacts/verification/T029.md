# T029 acceptance evidence

Requirements: `PRD-014`, `PRD-015`, `SRS-018`, `SRS-019`.

Verified on 2026-08-23.

The evaluator reads only named runtime inputs before producing each prediction, then loads that case's ground truth. DEV remains the default. HOLDOUT requires both `--confirm-frozen` and a verified release-freeze manifest; the benchmark loader independently verifies the frozen manifest digest, complete file set, byte counts, and per-file hashes.

Before HOLDOUT, the 120-case DEV evaluator completed, its artifact passed the JSON Schema, and the full pre-holdout gate passed with 164 backend and 8 frontend tests. `artifacts/release/freeze-v1.json` then recorded:

- code bundle SHA-256: `77e178b83e427fc4d5328cef1aa15582b5e22ae087088d258d17a7061a519996`;
- extractor/config SHA-256: `24ce16ba08a8b4edcbb843aa9fc9720e8f1db70358ffba22430e6251344f45b1`;
- frozen holdout manifest SHA-256: `1c285947c38bd0623b56cfb156dcc2eb3157505e5b8fc8bca45c089158ab3681`;
- Git provenance: `UNAVAILABLE_NOT_A_GIT_REPOSITORY` (reported honestly; no commit was invented).

One final confirmed HOLDOUT evaluation was then executed. It wrote `results/holdout-regex-v1-20260823-final.json` plus its exact-byte digest sidecar. Artifact SHA-256: `15349fd24f2fbceb1c6a38edafee92d5953f22af2e9611efcda17ba20f1992b8`. The saved artifact passed its closed Pydantic contract, JSON Schema, and sidecar verification.

The selected offline/regex-only system is explicitly B0 (`regex-baseline-v1` + the deterministic verifier). The artifact does not claim a model-backed B1 result. Its baseline comparison is therefore the identical B0 protocol and every delta is zero. All data and results are labeled synthetic.

No detector, prompt, schema, policy, or evaluator code was changed after the release freeze, and the HOLDOUT was not rerun.
