# T016 acceptance evidence

Requirements: `PRD-015`, `SRS-018`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_benchmark_generator.py -q
```

Observed result: 5 passed. The generator reproducibly creates 120 DEV and 60 HOLDOUT synthetic cases, with family-disjoint splits, 40/40/40 DEV and 20/20/20 HOLDOUT PASS/REVIEW/BLOCK label balance, committed ground truth, exact-source claim spans, and adversarial hard-negative families. It refuses to overwrite an existing dataset version.

The canonical generated dataset is at `data/benchmark/v1` and remains marked `frozen: false` until T017. No detector or evaluation path was run against HOLDOUT during this task.

Full `scripts/check.ps1` result: 118 backend tests and 1 frontend test passed; backend and frontend format, lint, strict typecheck, production build, and test gates passed with exit code 0.
