# T023 acceptance evidence

Requirements: `PRD-012`, `PRD-018`, `SRS-014`, `SRS-015`, `SRS-022`, `API-005`, `API-006`, `API-007`.

Verified on 2026-08-23.

The backend derives required inspection refs from material finding `claim_refs` and `structured_refs`; it rejects unrelated or incorrectly bound refs. `POST /inspect` appends idempotent source inspections. `POST /override` requires current BLOCK, every derived ref inspected by the fixed demo operator, an allowlisted structured reason, and a non-empty note only for OTHER. It appends the override and changes only `workflow_status` to `READY_WITH_OVERRIDE`. The historical BLOCK decision is unchanged. `POST /mark-ready` permits PASS or a completed override and returns `network_write_performed: false`.

The accessible override dialog moves focus to its heading, returns focus on close, lists the material finding sources, keeps submit disabled until both communication and structured ledger refs are acknowledged, requires the local-only confirmation, and exposes no arbitrary character hurdle. After override, the UI still displays `LOCAL HOLD` while enabling the explicitly local mark-ready action.

```powershell
python -m pytest backend/tests/test_case_api.py -q
cd frontend
npm test
```

Observed results: 8 backend API tests and 5 frontend workflow tests passed. Full `scripts/check.ps1` result: 141 backend tests and 5 frontend tests passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
