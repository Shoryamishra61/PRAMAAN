# T007 acceptance evidence

Requirements: `PRD-001`, `SRS-002`, `PRD-NFR-005`, `API-001`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_security.py -q
```

Observed result: 8 passed.

Tests cover valid exact raw bytes, missing and malformed signatures, post-signature payload mutation, exact whitespace semantics, non-disclosure of the secret in errors, and use of `hmac.compare_digest` for constant-time comparison.

Full `scripts/check.ps1` result: 45 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

