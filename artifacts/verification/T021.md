# T021 acceptance evidence

Requirements: `PRD-011`, `SRS-008`.

Verified on 2026-08-23.

The frontend source-navigation test selects a grounded claim, renders a visible `<mark>` from the backend-provided `span_start`/`span_end`, moves keyboard focus to that exact element, and emits a concise status announcement. It does not search, fuzzy-match, or re-infer source text in the browser. An intentionally incorrect test offset was rejected by the UI and corrected to the exact 36-character fixture span.

```powershell
cd frontend
npm test
```

Observed result: 3 tests passed. Full `scripts/check.ps1` result: 137 backend tests and 3 frontend tests passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
