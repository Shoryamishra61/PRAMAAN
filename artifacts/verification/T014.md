# T014 acceptance evidence

Requirements: `PRD-014`, `AI-012`, `AI-013`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_regex_baseline.py -q
```

Observed result: 10 passed.

The baseline is explicitly identified as `regex-baseline-v1` with no model ID. It emits the same strict claim schema and runs through the same grounding pipeline. Tests cover processed amount/reference extraction, request-vs-approval distinction, partial amount preservation, request allowlisting, negation, “should have been,” review-vs-promise, invoice distractor, and prompt-injection hard negatives.

The first targeted run found that `ref` matched the prefix of `refund`, producing `und` as a reference. The reference rule now requires a complete label token plus whitespace/delimiter, with RF-101 and partial-refund cases retained as regression tests.

Full `scripts/check.ps1` result: 107 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

