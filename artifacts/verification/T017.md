# T017 acceptance evidence

Requirements: `PRD-015`, `SRS-018`.

Verified on 2026-08-23.

The one-way freeze wrote `data/benchmark/v1/holdout-manifest.json` with a SHA-256 and byte count for every holdout file. Its committed root digest is:

```text
1c285947c38bd0623b56cfb156dcc2eb3157505e5b8fc8bca45c089158ab3681
```

`data/benchmark/v1/dataset.json` is marked `frozen: true`. The verifier checks the manifest digest, dataset identity, complete file set, file sizes, and per-file digests. SHA-256 is used only to detect byte changes relative to this recorded freeze; it is not described as authenticity or immutability.

```powershell
python -m pytest backend/tests/test_benchmark_integrity.py -q
python scripts/benchmark_cases.py --dataset data/benchmark/v1
```

Observed results: 5 tests passed. Mutation and unexpected-file injections were detected. HOLDOUT access without explicit `--confirm-frozen` was rejected. The default command returned exactly 120 DEV case IDs and has no default HOLDOUT path.

The detector was not run against frozen HOLDOUT. Full `scripts/check.ps1` result: 123 backend tests and 1 frontend test passed; all format, lint, strict typecheck, production build, and test gates passed with exit code 0.
