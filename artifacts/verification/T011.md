# T011 acceptance evidence

Requirements: `PRD-005`, `SRS-006`, `SRS-007`, `AI-001`, `AI-002`, `AI-003`, `AI-006`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_extraction.py -q
```

Observed result: 10 passed.

Every extracted claim validates against `contracts/grounded-claim.schema.json`. Closed Pydantic models reject decision status, confidence, source offsets, and tool-call fields. Backend validation binds claims to the requested document and allowlist. The async protocol exposes only `extract(request)` and the adversarial-text test verifies no tools, database, or secrets surface.

The request contract accepts exactly the v1 synthetic `text/plain` and `application/json` media types and rejects `application/pdf`; PDF/OCR remains out of scope.

Per the operator's explicit credential choice, no OpenAI API adapter is implemented. Executable extraction modes are limited to the regex baseline and visibly labeled offline replay adapters delivered by T014/T025.

Full `scripts/check.ps1` result: 69 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.
