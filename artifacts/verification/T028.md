# T028 acceptance evidence

Requirements: `PRD-NFR-001`, `PRD-NFR-003`, `PRD-NFR-004`.

Verified on 2026-08-23.

`RUNBOOK.md` documents the fresh-Windows-checkout sequence. `scripts/setup.ps1` creates a local virtual environment, installs the pinned Python project/dev constraints, and runs `npm ci`. `scripts/check.ps1` independently selects that environment and runs spec/package validation, the no-write guard, Python formatting/lint/strict mypy/tests, frontend formatting/lint/TypeScript production build, and frontend tests.

`scripts/demo.ps1` resets only the exact ignored `var/demo.sqlite3`, replays three correctly signed Razorpay-compatible synthetic fixtures through the inbound API, runs the versioned offline extraction cache through grounding and deterministic policy, verifies expected PASS/REVIEW/BLOCK outputs, persists complete analyst workspaces, then starts the backend and frontend. It records exact PIDs plus start-time ticks; `scripts/stop-demo.ps1` refuses a reused PID and stops only those recorded processes.

The live command was exercised successfully. `GET /api/v1/health` returned database `ready`, worker `idle`, and inference mode `offline`; `GET /api/v1/cases` returned the synthetic REVIEW, BLOCK, and PASS cases with preserved raw reason codes; the frontend returned its runnable HTML. Targeted integration coverage also traverses exact quote spans and the full BLOCK inspect → structured local override → local mark-ready path with no network write.

Money/minor-unit and timezone property tests remain in the full suite. Frontend tests verify text-labeled states, exact-source keyboard focus, dialog focus, Escape close/focus restoration, and the inspection precondition. This is automated pattern coverage, not a claim of WCAG certification.

Observed full result: package/spec validation and the no-write guard passed; 160 backend tests and 8 frontend tests passed; all format, lint, strict typecheck, and build gates passed. The frozen HOLDOUT was not evaluated.

Final release reproduction addendum: an isolated clean-source copy excluded Python/Node environments, build output, Hypothesis/test/type/lint caches, and runtime state. The initial reproduction attempt exposed a PATH Python without `venv`; after the root-cause setup fix, a cache-free copy completed setup, 169 backend tests, 9 frontend tests, the production build, and the offline technical rehearsal under freshly resolved Python 3.14 dependencies. The exact verified temporary copy was moved to the Windows Recycle Bin. The HOLDOUT was not loaded or rerun.
