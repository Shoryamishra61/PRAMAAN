# T006 acceptance evidence

Requirements: `PRD-007`, `SRS-012`.

Verified on 2026-08-23.

```powershell
python -m pytest backend/tests/test_decision.py -q
```

Observed result: 5 passed, including a Hypothesis property.

The decision model validates against `contracts/gate-decision.schema.json`, normalizes evaluation time to UTC, emits PASS only for an empty finding set, emits REVIEW for review-only findings, and emits BLOCK only when the verifier has produced a verified BLOCK finding. `primary_reason_code` is derived API/storage metadata and is intentionally excluded from the closed JSON contract. Canonical UI copy describes BLOCK as a local hold and every state as decision support rather than outcome prediction.

Full `scripts/check.ps1` result: 37 backend tests and 1 frontend test passed; all format, lint, strict typecheck, and build gates passed with exit code 0.

